# Exposures for aggregate mart models
# These exposures depend on aggregate models

exposures:
  - name: returned_orders
    type: dashboard
    maturity: high
    config:
      enabled: "{{ var('run_designed_to_fail', false) }}"
      tags: ['designed_to_fail']
    url: https://app.hex.tech/fishtown/app/Returned-Orders-032AHM0QS6YL3m2PDFYhul/latest
    description: Tracks how return rate is trending over time

    depends_on:
      - ref('fct_agg_returned_orders_by_month')

    owner:
      name: Data Team
      email: data@acme.co
